# Secrets
.env
.env.local
.env.*.local
*.key
*.pem
secrets/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
.pnpm-store/

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Claude Code session state (per-developer, not shared)
.claude/

# Logs & runtime
logs/
*.log
sessions/
*.lock
!package-lock.json
!pnpm-lock.yaml
# Runtime attachments storage (under data/uploads/) — already covered by `data/`
# below. The earlier blanket `attachments/` rule mistakenly hid the
# `core/attachments/` source module, so we don't use it.

# Database
*.db
*.sqlite
*.sqlite3
data/
db_data/

# Docker
.docker/

# Coverage
coverage/
htmlcov/
.coverage
.coverage.*

# Build artifacts for the installer
installer/dist/
installer/build/
*.exe
!installer/*.exe.placeholder
