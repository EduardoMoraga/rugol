# Production overlay — adds Postgres + Redis. Use with:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  db:
    image: postgres:16-alpine
    container_name: rugol-db
    environment:
      POSTGRES_USER: rugol
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rugol}
      POSTGRES_DB: rugol
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rugol"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: rugol-redis
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  core:
    environment:
      - DATABASE_URL=postgresql+asyncpg://rugol:${POSTGRES_PASSWORD:-rugol}@db:5432/rugol
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  db_data:
